RULE CHANGE UNDER REVIEW: A-2-3 straight placement made config-driven (config.ace_low_rank: lowest|second|highest, default lowest, TBC-flagged A23-RANK).
Evidence: exhaustive differential test of our evaluate_hand vs esrrhs/teenpatti_algorithm semantics over all C(52,3)=22,100 hands. Category order identical (trail>pure>seq>color>pair>high), pair/trio kickers identical, no suit ranking either side. ONLY divergence: A-2-3. Ours(lowest) ranks it below 2-3-4; reference (Ace-always-14) ranks it just below A-K-Q; a third live variant (Teen Patti Gold traditional) ranks it above A-K-Q. Our engine now supports all three via ace_low_rank; "second" mode verified 0 divergences vs reference; "lowest"/"highest" diverge in exactly the 2 A-2-3 orderings (straight + straight-flush), as intended. Default unchanged (lowest, modern-casino style). Room.calculate_result threads config.ace_low_rank; settlements stamp config_version so any future flip is auditable per-round.
Question: is this change safe, complete, and correctly defaulted?
