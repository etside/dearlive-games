ARCHITECTURE UNDER REVIEW: engine plugin registry + skill/hook system + lookup-table evaluator upgrade.
1. common/plugins.py: EnginePlugin dataclass (id/name/version/status/tbc/factory); @engine_plugin decorator (duplicate-id raises); get() fails closed on unknown ids (no fallback game); create() refuses non-live; catalog(); import_builtin_games() imports 3 game plugin modules. Teen Patti live 1.1.0; Greedy + Animal Wheel registered planned with TBC lists.
2. common/skills.py: Skill base (skill_id, handles subset of 6 hooks, run()); SkillBus register (duplicate/unknown-hook rejected), enable/disable per game (unknown skill KeyError), emit() post-commit fan-out with per-skill try/except (failures audited, never raise). Skills get dict copies. WebhookFanoutSkill bundled but NOT auto-enabled (avoids double delivery with existing _fire path).
3. Service integration: optional SkillBus param; _skill() wrapped in try/except so observers can never fail money paths; 6 hooks emitted after commit points (round_start, bet, close, result, settle, cancel).
4. Lookup table (games/teen_patti_pro/table.py): 22,100 no-joker hands x 3 A23 modes built from live evaluate_hand (single source of truth, parity by construction, full-parity test over all hands); reference-style key encoding + dense position index; lazy thread-safe build; score_hand() uses table with fail-open fallback to live evaluator on any error; jokers bypass via best_expansion.
Question: registry/skill/table safety and correctness (fail-closed behavior, isolation, fallback soundness)?
