[
  {"key": "lock_covers_all_mutation", "type": "noul", "instructions": "Is there any mutation of round state (bets, status, settlements, carry) reachable WITHOUT holding the room lock?"},
  {"key": "settle_replay_safe", "type": "noul", "instructions": "Can settle() ever double-pay (same bet credited twice) through replay, concurrent calls, or the _settled flag path?"},
  {"key": "debit_compensation_hole", "type": "noul", "instructions": "Is there a path where a wallet debit happens but the player is neither given a bet nor refunded (lost funds)?"},
  {"key": "tie_split_bug", "type": "noul", "instructions": "Does the tie-split logic mis-allocate (wrong position shares, lost dust, or negative carry)?"},
  {"key": "conservation_bug", "type": "noul", "instructions": "Can payouts + carry_out ever NOT equal the pot (money created or destroyed)?"},
  {"key": "card_leak_bug", "type": "noul", "instructions": "Can hole cards or seed leak to a viewer before RESULT through snapshot, events_since, or result endpoints?"},
  {"key": "idempotency_hole", "type": "noul", "instructions": "Can the same idempotency key produce two debits or two bets (claim vs room-replay race)?"},
  {"key": "cancel_race", "type": "noul", "instructions": "Can cancel() race with calculate_result/settle to void an already-settled round or settle a cancelled round?"},
  {"key": "rank_eval_bug", "type": "noul", "instructions": "Does evaluate_hand mis-rank any standard 3-card Teen Patti hand (trail/pure-seq/seq/color/pair/high incl A-2-3 low)?"},
  {"key": "code_soundness", "type": "score", "instructions": "Overall code soundness?",
   "criteria": ["blocking bugs present, must fix before use", "minor issues only, fix then proceed", "no significant issues found"]},
  {"key": "top_concern", "type": "choice", "instructions": "Top concern area?",
   "criteria": {"concurrency": "locks / races / replay", "money": "debit / settlement / conservation", "rules": "ranking / tie-split / deal", "visibility": "card leak / snapshot redaction"}}
]
